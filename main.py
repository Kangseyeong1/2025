# streamlit run main.py 로 실행하세요
# -*- coding: utf-8 -*-
import streamlit as st
import math
import re
from collections import defaultdict

# =====================================
# 기본 설정
# =====================================
st.set_page_config(page_title="화학식 정보 사전", page_icon="🧪")
st.title("🧪 화학식 정보 사전 (고등학생용)")
st.write("예: H2O, CO2, NaCl 같은 화학식이나 '물', '이산화탄소', '소금' 같은 한글 이름을 입력하면 정보를 보여줍니다.")

# =====================================
# 원자량 데이터 + 한국어 이름
# =====================================
ATOMIC_DATA = {
    'H': (1.008, '수소'), 'C': (12.011, '탄소'), 'N': (14.007, '질소'), 'O': (15.999, '산소'),
    'Na': (22.990, '나트륨'), 'Mg': (24.305, '마그네슘'), 'Al': (26.982, '알루미늄'), 'Si': (28.085, '규소'), 'P': (30.974, '인'),
    'S': (32.06, '황'), 'Cl': (35.45, '염소'), 'K': (39.098, '칼륨'), 'Ca': (40.078, '칼슘'), 'Fe': (55.845, '철'),
    'Cu': (63.546, '구리'), 'Zn': (65.38, '아연')
}

# =====================================
# 간단 화학식 파서
# =====================================
TOKEN = re.compile(r"([A-Z][a-z]?|\(|\)|\d+)")

def parse_formula(formula: str):
    tokens = TOKEN.findall(formula.replace(' ', ''))
    stack = [defaultdict(int)]
    i = 0
    def add(sym, n):
        stack[-1][sym] += n
    while i < len(tokens):
        t = tokens[i]
        if t == '(':
            stack.append(defaultdict(int))
            i += 1
        elif t == ')':
            i += 1
            mult = 1
            if i < len(tokens) and tokens[i].isdigit():
                mult = int(tokens[i]); i += 1
            group = stack.pop()
            for k, v in group.items():
                add(k, v * mult)
        elif t.isdigit():
            raise ValueError("숫자가 앞에 오는 표기는 지원하지 않습니다.")
        else:
            sym = t; i += 1
            mult = 1
            if i < len(tokens) and tokens[i].isdigit():
                mult = int(tokens[i]); i += 1
            add(sym, mult)
    if len(stack) != 1:
        raise ValueError("괄호 처리 오류")
    return dict(stack[0])

# =====================================
# 내장 화합물 정보 (상세)
# =====================================
COMPOUNDS = {
    "H2O": {"이름": "물", "상태(상온)": "액체", "종류": "산화물", "설명": "생명체에 필수적인 극성 용매로 많은 물질을 용해시킵니다.",
            "물리적 성질": "끓는점 100℃, 어는점 0℃, 밀도 1 g/cm³", "안전": "보통 안전하지만 전기와 함께 사용 시 감전 위험 있음."},
    "CO2": {"이름": "이산화탄소", "상태(상온)": "기체", "종류": "산화물", "설명": "호흡과 연소의 산물, 광합성에 사용되며 드라이아이스 형태로도 존재.",
            "물리적 성질": "무색 무취, 드라이아이스는 -78.5℃에서 승화", "안전": "고농도는 질식 위험. 환기 필요."},
    "NaCl": {"이름": "염화 나트륨(소금)", "상태(상온)": "고체", "종류": "이온결합 화합물", "설명": "바닷물과 암염의 주성분, 음식 조리에 사용.",
             "물리적 성질": "무색 결정, 녹는점 801℃", "안전": "과다 섭취는 건강에 해로움."},
    "NH3": {"이름": "암모니아", "상태(상온)": "기체", "종류": "염기성 화합물", "설명": "자극적인 냄새가 나는 기체, 비료와 세정제 제조에 사용.",
            "물리적 성질": "무색 자극성 기체, 끓는점 -33℃", "안전": "고농도는 호흡기 자극 및 위험."},
    "CH4": {"이름": "메테인", "상태(상온)": "기체", "종류": "탄화수소", "설명": "천연가스의 주성분, 연료로 사용.",
            "물리적 성질": "무색 무취 기체, 끓는점 -161℃", "안전": "가연성이 높음."},
    "C2H5OH": {"이름": "에탄올", "상태(상온)": "액체", "종류": "알코올", "설명": "알코올 음료의 성분, 소독제로도 사용.",
               "물리적 성질": "무색 액체, 끓는점 78℃", "안전": "섭취 시 알코올 중독 위험."},
    "H2SO4": {"이름": "황산", "상태(상온)": "액체", "종류": "산", "설명": "매우 강한 산, 비료와 화학 산업에서 광범위하게 사용.",
              "물리적 성질": "무색-갈색 점성 액체, 끓는점 337℃", "안전": "부식성이 매우 강해 화상 위험."},
    "HCl": {"이름": "염화수소(염산)", "상태(상온)": "기체(수용액은 염산)", "종류": "산", "설명": "강한 산성을 띠며 금속과 반응.",
            "물리적 성질": "무색 자극성 기체", "안전": "강산으로 부식성이 강함."},
    "CaCO3": {"이름": "탄산칼슘", "상태(상온)": "고체", "종류": "염", "설명": "석회석, 대리석, 조개껍질의 주성분.",
              "물리적 성질": "흰색 고체, 녹는점 825℃", "안전": "상대적으로 안전."},
    "NaHCO3": {"이름": "탄산수소나트륨(베이킹소다)", "상태(상온)": "고체", "종류": "염", "설명": "제과, 세정, 완충작용 등에 사용.",
               "물리적 성질": "흰색 결정성 분말", "안전": "대체로 안전."},
}

# =====================================
# 한글 이름 → 화학식 변환
# =====================================
NAME_TO_FORMULA = {
    "물": "H2O", "이산화탄소": "CO2", "소금": "NaCl", "염화나트륨": "NaCl",
    "암모니아": "NH3", "메테인": "CH4", "에탄올": "C2H5OH",
    "황산": "H2SO4", "염산": "HCl", "탄산칼슘": "CaCO3", "석회석": "CaCO3",
    "베이킹소다": "NaHCO3", "탄산수소나트륨": "NaHCO3"
}

# =====================================
# 지원되는 화합물 리스트 표시
# =====================================
st.subheader("📖 검색 가능한 화합물 목록")
for f, info in COMPOUNDS.items():
    st.write(f"- {info['이름']} ({f})")

# =====================================
# 사용자 입력
# =====================================
user_input = st.text_input("화학식 또는 한글 이름을 입력하세요:")
if user_input:
    formula = NAME_TO_FORMULA.get(user_input, user_input)
    if formula not in COMPOUNDS:
        st.error("해당 화합물은 데이터베이스에 없습니다.")
    else:
        info = COMPOUNDS[formula]
        st.subheader("기본 정보")
        st.write(f"**이름**: {info['이름']} ({formula})")
        st.write(f"**상태(상온)**: {info['상태(상온)']}")
        st.write(f"**종류**: {info['종류']}")
        st.write(f"**설명**: {info['설명']}")
        st.write(f"**물리적 성질**: {info['물리적 성질']}")
        st.write(f"**안전**: {info['안전']}")

        # 원소 조성 분석
        try:
            comp = parse_formula(formula)
            st.subheader("원소 조성 및 몰질량")
            total_mass = 0
            rows = []
            for el, count in comp.items():
                if el in ATOMIC_DATA:
                    mass, kr = ATOMIC_DATA[el]
                    subtotal = mass * count
                    total_mass += subtotal
                    rows.append((f"{el} ({kr})", count, subtotal))
            st.table({"원소": [r[0] for r in rows], "개수": [r[1] for r in rows], "질량(g/mol)": [r[2] for r in rows]})
            st.write(f"**총 몰질량**: {total_mass:.3f} g/mol")
        except Exception as e:
            st.error(f"원소 분석 오류: {e}")
